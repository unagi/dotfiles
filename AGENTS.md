# dotfiles AGENTS

## 最優先ガードレール：実環境への反映はユーザーが管理する
- このリポジトリの開発者責務は、リポジトリ内のsourceと保守用ファイルの編集・検証・レビューまでとする。chezmoiの実行と実環境への反映は本リポジトリの開発者責務に含めず、ユーザーが管理する別作業とする。
- 実環境への反映を開発の残作業・完了条件として扱わない。開発完了時にchezmoiの実行や配布先更新を提案したり、その承認を求めたりしない。対象と操作を指定したユーザーからの別途明示依頼がある場合だけ、その依頼の範囲で扱う。
- ユーザーが対象と操作を明示して指示しない限り、`~/.codex/`、`~/.claude/`などの配布先・実環境のファイルを直接作成・変更・削除してはならない。コピー、スクリプト、他ツール経由でも同じである。
- ユーザーの明示的な実行指示がない限り、`chezmoi`を実行してはならない。`apply`だけでなく、`diff`、`execute-template`、`--dry-run`などの確認・検証目的の実行も含む。必要な検証は既存CIに委ねるか、chezmoiを使わない範囲で行い、未検証部分を報告する。
- 「修正して」「反映して」「コミットして」「pushして」などのリポジトリ作業の依頼を、配布先の更新やchezmoi実行の許可と解釈しない。README等の適用手順、一般的な自律実行・検証方針も許可の根拠にはならない。
- このガードレールは本リポジトリ内の通常の作業・検証手順より優先する。許可のない操作は試行せず、実行権限があっても行わない。サンドボックス拒否を受けてから昇格を求める進め方にせず、拒否された操作を別経路で迂回しない。

## 目的
- このリポジトリは `chezmoi` の source として、ユーザーグローバル設定を管理する。
- リポジトリ保守用ファイルは配布対象ではなく、`chezmoi` 管理外にする。

## 復元先
- `dot_codex/` → `~/.codex/`
- `private_dot_claude/` → `~/.claude/`

## Codex 管理方針
- `~/.codex` は「明示的に管理する固定資産」と「Codex エコシステムが内部管理する状態ファイル」が混在する。
- `chezmoi` で管理するのは、移植性があり、意図的に配布したいファイルだけに限定する。
- `common/` と `languages/` のような共通ガイドは、`dot_codex/` 配下で明示管理する。
- built-in / stateful な領域は `chezmoi` に取り込まない。部分同期すると整合性が崩れやすいため。

## Codex で除外するもの
- `config.toml`
- 認証情報、履歴、ログ、キャッシュ、DB、テンポラリ、セッション
- `skills/` 配下全体
- Codex が内部的に生成・更新するファイル

## 運用ルール
- 指示・テンプレート・説明資料を追加・変更・整理する場合は、先に [指示文書の保守ルール](docs/instruction-maintenance.md) を読み、文書の責務・正本・参照関係に従う。
- `~/.codex` 配下の新規ファイルを取り込む前に、「ユーザーが保守する静的資産」か「Codex が管理する状態」かを先に判定する。
- 判定に迷うものは、先に `AGENTS.md` と `.chezmoiignore` の方針を更新してから取り込む。
- ルート直下の `AGENTS.md`、`README.md`、`LICENSE` などのリポジトリ保守用ファイルは、`.chezmoiignore` に明示する。
