# vim: ft=zsh

function use-ssh-agent-on-cygwin {
    local args="$@"
    ssh-add -l >& /dev/null
    if [ $? = 2 ]; then
        echo "ssh-agent starting..."
        eval `ssh-agent`
        ssh-add ~/.ssh/id_rsa
    fi
    ssh $args
}
